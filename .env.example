# 이 파일을 복사해 `.env` 로 저장한 뒤 값을 채우세요.
# `.env` 는 .gitignore 로 GitHub 에 포함되지 않습니다.
#
# NEIS Open API 키 발급: https://open.neis.go.kr/portal/guide/apiGuidePage.do
#
# Cloudflare Pages 배포 시: 대시보드에서 동일 이름(VITE_NEIS_API_KEY) 환경 변수를
# Production / Preview 빌드에 설정해야 합니다. (아래는 채팅 안내 참고)
#
VITE_NEIS_API_KEY=
